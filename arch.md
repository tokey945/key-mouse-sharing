# 架构文档 (Architecture Documentation)

## 1. 项目概述
本项目是一个基于 Tauri + Vue 3 + TypeScript 的键鼠共享工具，旨在实现跨设备的键鼠控制功能。项目主要分为控制端（发送）和客户端（接收）两种角色，支持键鼠共享、文件共享和屏幕共享功能（后两者待开发）。

## 2. 技术栈
- **前端框架**: Vue 3 (Composition API + `<script setup>`)
- **构建工具**: Vite
- **UI 组件库**: 自定义组件（基于 Tailwind CSS）
- **状态管理**: Vue Router + 组件状态
- **后端通信**: Tauri (Rust 后端)
- **类型系统**: TypeScript

## 3. 核心模块

### 3.1 键鼠共享模块
- **功能描述**: 实现键鼠事件的捕获、传输和模拟。
- **技术实现**:
  - 使用 Tauri 提供的 API 与 Rust 后端通信。
  - 前端通过 `@tauri-apps/api` 调用后端方法。
  - 日志系统用于记录操作状态。

### 3.2 文件共享模块（待开发）
- **功能描述**: 实现文件的传输和接收。
- **技术实现**: 待定。

### 3.3 屏幕共享模块（待开发）
- **功能描述**: 实现屏幕画面的传输和显示。
- **技术实现**: 待定。

## 4. 项目结构
```
├── src/
│   ├── main.ts                # 应用入口
│   ├── App.vue                # 根组件
│   ├── router/               # 路由配置
│   ├── pages/                 # 页面组件
│   │   ├── KeyMouseSharing.vue # 键鼠共享页面
│   │   ├── FileSharing.vue    # 文件共享页面（待开发）
│   │   └── ScreenSharing.vue # 屏幕共享页面（待开发）
│   ├── components/            # 公共组件
│   └── assets/                # 静态资源
├── vite.config.ts             # Vite 配置
├── package.json               # 项目依赖
└── README.md                  # 项目说明
```

## 5. 数据流
1. **键鼠事件捕获**: 前端捕获用户输入事件（如鼠标移动、键盘按键）。
2. **事件传输**: 通过 Tauri 后端将事件数据发送到目标设备。
3. **事件模拟**: 目标设备接收事件并模拟操作。

## 6. 依赖项
- **核心依赖**:
  - `@tauri-apps/api`: 用于与 Rust 后端通信。
  - `vue-router`: 路由管理。
  - `tailwindcss`: UI 样式。
- **开发依赖**:
  - `vite`: 构建工具。
  - `typescript`: 类型支持。

## 7. 后续计划
1. 完善文件共享和屏幕共享功能。
2. 优化键鼠事件的传输效率。
3. 增加多设备支持。
